# 第一篇：ESP32 與 Arduino C 基礎

目前範例：

- `01_hello`：以 GPIO 2 狀態 LED 驗證編譯、燒錄、重啟與主迴圈，不需要序列監控。
- `02_led`：使用 GPIO 2 進行數位輸出。

後續會加入數位輸入、類比輸入、PWM 與 WS2812。

如果指定課程板沒有 GPIO 2 板載 LED，必須外接適當限流電阻與 LED。第一篇只使用可直接觀察的硬體動作，不保留 Serial-only 訊息；第二篇會先建立 OLED 訊息介面，再開始感測器與網路章節。
